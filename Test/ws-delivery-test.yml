spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:7001/delivery_test
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 3000
